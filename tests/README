The tests are intended to be run using pytest.
https://docs.pytest.org/

To run tests, simply install pytest, and then, in the eventlet tree, do:
   $ pytest

That's it!  It tends to emit a lot of tracebacks from various poorly-behaving tests, but they still (generally) pass.
